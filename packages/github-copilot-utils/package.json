{
  "name": "@averlon/github-copilot-utils",
  "version": "1.0.0",
  "description": "Shared utilities for GitHub Copilot integration in Averlon actions",
  "main": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "build": "bun run --bun tsc --noEmit",
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "format": "prettier --write \"**/*.ts\""
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.1.1",
    "@averlon/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
