{
  "name": "@averlon/container-analysis-action",
  "version": "1.0.0",
  "description": "Averlon Container Analysis GitHub Action",
  "main": "src/main.ts",
  "scripts": {
    "test": "bun test",
    "build": "bun run --bun tsc --noEmit",
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "format": "prettier --write \"**/*.ts\""
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.1.1",
    "@anthropic-ai/sdk": "^0.116.0",
    "@averlon/github-actions-utils": "workspace:*",
    "@averlon/shared": "workspace:*",
    "fast-glob": "^3.3.2"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
